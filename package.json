{
  "name": "taskmark-taskmark",
  "private": true,
  "type": "module",
  "description": "Taskmark board \u2014 npm start / npx taskmark serve (local), or deploy on Vercel as a Node project (server.js).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "taskmark serve --no-open",
    "serve": "taskmark serve",
    "dev": "taskmark serve"
  },
  "dependencies": {
    "@taskmark/ui": "^0.1.0"
  }
}
